spring:
  main:
    web-application-type: none
  kafka:
    listener:
      auto-startup: false
  flyway:
    enabled: false

app:
  texas:
    token-introspection-endpoint: http://localhost:0/not-in-use